{
  "name": "@syncorswim/server",
  "version": "0.1.0",
  "private": true,
  "description": "syncorswim server.",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc --build",
    "dev": "npm run build --workspace @syncorswim/shared && tsx src/index.ts",
    "start": "node dist/index.js",
    "test": "vitest run --passWithNoTests",
    "typecheck": "tsc --build"
  },
  "dependencies": {
    "@syncorswim/shared": "0.1.0",
    "ws": "^8.21.0"
  },
  "devDependencies": {
    "@types/ws": "^8.18.1",
    "tsx": "^4.0.0"
  }
}
